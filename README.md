# Homework-3
Due 3/14

Add prompts to set desired contents of password

identify all possible characters to be used in password (uppercase/lowercase letter, number, symbols)

set functions to retreive characters from character board

identify all retreival functions as signle variable

identify all possible characters as types and create array for said types

create a for loop using which selects a character from each array until the desired user input length is acheived

make sure final password is linked to the provided writePassword function

